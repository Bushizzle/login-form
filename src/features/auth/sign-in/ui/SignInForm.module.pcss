.form {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-lg);
  width: 100%;
  max-width: 24rem;
}

.alert {
  margin-bottom: var(--spacing-sm);
}

.submitButton {
  margin-top: var(--spacing-sm);
  width: 100%;
}


